---
interface Props {
  level?: 1 | 2 | 3 | 4 | 5 | 6;
  className?: string;
}

const { level = 1, className = '' } = Astro.props;
const Tag = `h${level}` as keyof HTMLElementTagNameMap;
---

<Tag class:list={['text-white font-bold', className]}>
  <slot />
</Tag>